[package]
name = "display-rest"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
name = "display_rest"
path = "src/lib.rs"

[dependencies]
display-core = { path = "../core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
