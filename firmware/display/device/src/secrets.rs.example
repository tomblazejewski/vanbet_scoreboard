// Copy this file to secrets.rs (gitignored, never committed) and fill in
// your actual WiFi credentials — same convention as the ESP-NOW pre-shared
// key in protocol.md's secrets.h.example.

pub const WIFI_SSID: &str = "your-network-name";
pub const WIFI_PASSWORD: &str = "your-network-password";
